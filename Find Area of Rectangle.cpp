/*Problem Statement
Take the length(L) and breadth(B) of the rectangle as input and find its area.*/

#include <iostream>
using namespace std;

int main() {
    //Write your code here
int length, breadth;
    int area;
    
    cin>>length;
    cin>>breadth;
    area= length*breadth;
    cout<<area;
    
    
    return 0;
}
